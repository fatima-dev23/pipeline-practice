#name
name: Second Pipeline

#when starts

on:
  push:
    branches:
      - main

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Learning
        run: echo "Learnt Pipelines"